#include <stdio.h>
#include <math.h>
int main() {
   char operator;
   double num1, num2, result;

   printf("Enter an operator (+, -, *, / ");
   scanf("%c", &operator);

   switch(operator) {
      case '+':
      case '-':
      case '*':
      case '/':
   }

   return 0;
}